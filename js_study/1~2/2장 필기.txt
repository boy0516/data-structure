어레이가 딘다. 투 바이 .

list 한번더 해보고, days 해픈 

player insite 인더 콘솔. 

console.log() 그쵸 ..

function. 

console.log (2+2)
plus

prompt는 너무 오래전의 방법이고 페이지 로드를 멈추기 때문에 사용하지 않는다. 

사람들은 더는 쓰지 않는다. 

그래도 기본 원리를 익히기 위 사용해본다면 NaN이렇게 된다.

15퍼센트로 되겠다. 

장난같은 프로젝트가 아니라 최종적으로 괜찮은 프로젝트라고 해도 괜찮겠다. 

paresInt()를 이용하면 받은 문자를 숫자로 변환 할 수 있는데 NaN라고 뜨는것은 문자가 숫자가 아니라 문자일때 발생한다. 


다음은 isNaN() 함수에 대해 알아보겠다.

isNaN함수를 이용하면 입력값을 boolen값으로 반환해준다. 

true false로 반환한다. 

이러한 값을 이용해서 if문에 넣어줌으로써 사용할 수도 있을것이다. 








